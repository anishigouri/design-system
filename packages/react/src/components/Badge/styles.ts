import { styled } from '../../styles'

export const BadgeContainer = styled('div', {
  borderRadius: '$lg',
  padding: '2px 8px',
  fontWeight: '$medium',
  fontSize: '$sm',
  textAlign: 'center',
  display: 'inline-block',
})
